# psycopg2 nécessite des bibliothèques natives (libpq) : vendorisé pour
# Amazon Linux via `make build-tenders-api-lambda` (voir infra/terraform/Makefile),
# jamais un simple `pip install` local packagé tel quel dans le zip Lambda.
# boto3 est déjà présent dans le runtime Lambda managé — inutile de le packager.
psycopg2-binary==2.9.10
